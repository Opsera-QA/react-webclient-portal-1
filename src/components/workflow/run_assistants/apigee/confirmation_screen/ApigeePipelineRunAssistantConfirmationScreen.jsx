import React from 'react';
import PropTypes from "prop-types";

// TODO: Implement
const ApigeePipelineRunAssistantConfirmationScreen = ({isSaving, isLoading, apigeeRunParametersModel}) => {
};

ApigeePipelineRunAssistantConfirmationScreen.propTypes = {
  apigeeRunParametersModel: PropTypes.object,
  isSaving: PropTypes.bool,
  isLoading: PropTypes.bool,
};

export default ApigeePipelineRunAssistantConfirmationScreen;